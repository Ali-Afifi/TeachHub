@model dynamic

@{
	ViewData["Title"] = "Index";
} 

<h1>Students</h1>

<table class="table">
	<thead>
		<tr>
			<th>ID</th>
			<th>First Name</th>
			<th>Last Name</th>
			<th>Birth Date</th>
			<th>Gender</th>
			<th>User Name</th>
			<th></th>
		</tr>
	</thead>
	<tbody>
		@foreach (var user in Model.Users)
		{
			<tr>
				<td>@user.ID</td>
				<td>@user.FirstName</td>
				<td>@user.LastName</td>
				<td>@user.BirthDate.ToString("yyyy/MM/dd")</td>
				<td>@user.Gender</td>
				<td>@user.UserName</td>
				<td>
					<a asp-action="Details" asp-route-id="@user.ID">Details</a> |
					<a asp-action="Enroll" asp-route-id="@user.ID">Enroll</a>
				</td>
			</tr>
		}
	</tbody>
</table>

